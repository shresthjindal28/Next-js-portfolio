// SectionLoader component
import React from 'react';

const SectionLoader = () => {
  return (
    <div>
      {/* Loader for section */}
      <span>Loading...</span>
    </div>
  );
};

export default SectionLoader;
